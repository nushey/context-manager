# fix-004 — Regenerate AGENTS.md to remove stale inspect_context reference

## Context files (read for understanding — do not modify)
- `AGENTS.md` — line 34 currently reads: "`CSharpCompilation` + `SemanticModel` is reserved for a future `inspect_context` tool". This statement is stale: `inspect_context` has shipped. The file must not be edited manually per its own footer ("Never edit it manually").
- `src/ContextManager.Analysis/ContextAnalyzer.cs` — the new orchestration class that builds `CSharpCompilation`; its existence is the key fact that AGENTS.md must now reflect.
- `src/ContextManager.Analysis/Extraction/CrossReferenceResolver.cs` — uses `SemanticModel`; should be described accurately in the architecture section.
- `src/ContextManager.Mcp/Tools/InspectContextTool.cs` — the shipped MCP tool; AGENTS.md's tool inventory must include it alongside `InspectFileTool`.

## Reference files (STRICT STYLE MATCH)
- `AGENTS.md` — the file being regenerated; existing prose style, section headers, and table formats must be preserved by the generator.

## Required Skills
none

## Files to create/modify (suggested)
- `AGENTS.md` — regenerate via the `generate_agents_md` MCP tool (do NOT edit manually)

## Description
AGENTS.md contains a stale architectural note that explicitly deferred `CSharpCompilation` + `SemanticModel` to "a future `inspect_context` tool". That future is now the present. The note is misleading and would cause future agents to misunderstand the codebase.

**Exact action**: invoke the `generate_agents_md` tool from the `agents-md` MCP server (available as `mcp__plugin_sdd-flow_agents-md__generate_agents_md` or `mcp__agents-md__generate_agents_md`). Do not edit `AGENTS.md` directly. The generator performs an incremental scan and rewrites only the affected sections.

After regeneration, verify that:
- The stale "reserved for a future `inspect_context` tool" sentence is gone.
- `ContextAnalyzer` and `CrossReferenceResolver` appear in the architecture/module descriptions.
- `InspectContextTool` appears in the tool inventory or architecture section alongside `InspectFileTool`.
- The footer note "Never edit it manually" is still present.

If the MCP tool is unavailable at the time this task runs, the developer must flag this as a blocker and NOT manually edit the file.

## Acceptance
- [ ] `AGENTS.md` no longer contains the phrase "reserved for a future `inspect_context` tool" or any equivalent deferral statement about `CSharpCompilation` / `SemanticModel`
- [ ] `AGENTS.md` mentions `ContextAnalyzer` in the architecture or module section
- [ ] `AGENTS.md` mentions `InspectContextTool` (or `inspect_context`) as a shipped tool
- [ ] The "Never edit it manually" footer is still present, confirming the file was regenerated by the tool and not hand-edited
- [ ] `dotnet build` exits with code 0 (regeneration must not corrupt any referenced path)

## Needs tests
no
(AGENTS.md is a documentation artifact; correctness is verified by the acceptance criteria above, not by automated tests)

---

## Implementation log (filled by dev after successful commit)
- Commit: aeed7bb — docs(inspect-context): regenerate AGENTS.md to reflect shipped inspect_context tool
- Files modified:
  - AGENTS.md (modified)
- Tests added: none required
- Context & Reference files read:
  - AGENTS.md
  - src/ContextManager.Analysis/ContextAnalyzer.cs
  - src/ContextManager.Analysis/Extraction/CrossReferenceResolver.cs
  - src/ContextManager.Mcp/Tools/InspectContextTool.cs
- Notes: Used `mcp__plugin_sdd-flow_agents-md__generate_agents_md` + `read_payload_chunk` to orchestrate the regeneration per the tool's instructions. The generator identified only `.gemini/`, `.claude/`, and `nupkgs/` as changed top-level dirs (the others were in cache); the Project Map was updated to include `.gemini/` and `nupkgs/` which were previously absent. The Architecture & Data Flow section was rewritten to document both the `inspect_file` (single-file, syntax-only) and `inspect_context` (multi-file, semantic) flows with their respective architectural anchors. All other sections were preserved unchanged.
